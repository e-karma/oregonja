{{outlet}}
	<form class="post-form" {{action 'createTodo' on='submit'}}>
	    <div class="form-group post-banner">
	      	<div class="col-sm-4 col-sm-offset-4">
						{{input type="text" value=model.title class='form-control form-control-light' placeholder="Your Blog Post Title Goes Here"}}
	      	</div>
	    </div>
	    <div class="form-group">
	        {{textarea value=model.body class='form-control' placeholder="Tell Your Story Here"}}
	    </div>
			<div class="col-sm-offset-5">
				<button class="btn rb-btn-info" {{action 'createTodo' model}}>Create Todo</button>
			</div>
	</form>
